import { compare } from 'bcrypt';
import {  IsNotEmpty ,IsString,IsNumberString, IsEmail, Matches, MaxLength, MinLength, IsPhoneNumber, IsMobilePhone} from 'class-validator';
export class typeDatabase{
  id:number   
   
}
